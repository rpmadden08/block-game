package com.companyname.blockgame;

import java.awt.*;
import java.util.*;
import java.io.*;

import static com.companyname.blockgame.Constants.*;
import com.companyname.blockgame.BlockTypes.*;

public class MainMenu {
	
	
	public void tick() {
		
	}
	public void render(Graphics g) {
		
		Font font = new Font("Helvetica", Font.PLAIN, 12);
		BufferedImage img = new BufferedImage(1, 1, BufferedImage.TYPE_INT_ARGB);
		FontMetrics fm = img.getGraphics().getFontMetrics(font);
		int width = fm.stringWidth("Your string");
		
		g.setColor(Color.WHITE);
		g.drawString("NEW GAME",Component.initialGameWidth /2/Component.pixelSize, 24);
//		g.drawString("LOAD GAME",3, 24);
//		g.drawString("OPTIONS",3, 24);
//		g.drawString("QUIT",3, 24);
	}
}


