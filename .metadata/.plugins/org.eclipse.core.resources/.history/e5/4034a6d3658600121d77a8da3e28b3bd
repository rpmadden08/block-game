package net.ulixava.adventurecraft;

import java.awt.*;

public class Collectible {
	public int x = 0;
	public int y = 0;
	public Collectible(int x2, int y2) {
		x = x2;
		y = y2;		
	}
	
	public void isTouching() {
		
	}
	
	public void tick() {
		//If Collectible location is within the 4 locations than print collision...  
		//192 144
		int left = (int) Component.character.x + (int) Character.frameOffsetLeft + (Tile.tileSize /2); //193
		int right = (int) Component.character.x + (int) Character.frameOffsetRight + (Tile.tileSize /2);//206
		int top = (int) Component.character.y + (int) Character.frameOffsetTop - 7+ (Tile.tileSize /2); // 16
		int bottom = (int) Component.character.y + (int) Character.frameOffsetBottom+ (Tile.tileSize /2); 
		System.out.println("Left"+ left + "right" +right+ "top" +top+ "bottom" + bottom);
		if(x > left && x < right && y > top && y < bottom) {
			System.out.println("Collision detected");
		}

	}
	
	public void render(Graphics g) {
		g.drawImage(Tile.tileset_terrain,
				x - (int) Component.sX,
				y - (int) Component.sY,
				x + Tile.tileSize - (int) Component.sX,
				y + Tile.tileSize - (int) Component.sY,
				Tile.earthClump[0] * Tile.tileSize,
				Tile.earthClump[1] * Tile.tileSize,
				Tile.earthClump[0] * Tile.tileSize + Tile.tileSize,
				Tile.earthClump[1] * Tile.tileSize + Tile.tileSize,
				null);
	}
}
